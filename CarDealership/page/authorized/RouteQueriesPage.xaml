<Page x:Class="CarDealership.page.authorized.RouteQueriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Title="RoureQueriesPage">

    <Page.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3B82F6" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Height" Value="40" />
            <Setter Property="Margin" Value="0,14,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#60A5FA" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#1E40AF" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#a0aec0" />
                                <Setter Property="Foreground" Value="#6b7280" />
                                <Setter Property="Cursor" Value="Arrow" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="QueryCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White" />
            <Setter Property="CornerRadius" Value="14" />
            <Setter Property="Margin" Value="10" />
            <Setter Property="Padding" Value="18" />
            <Setter Property="Width" Value="320" />
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="Gray" ShadowDepth="2" Opacity="0.18" BlurRadius="5" />
                </Setter.Value>
            </Setter>
        </Style>
    </Page.Resources>

    <Grid Background="#f0f2f5">
        <StackPanel>
            <TextBlock Text="Вбудовані запити"
                       FontSize="28"
                       FontWeight="Bold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20" />

            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl>
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel HorizontalAlignment="Center" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.Resources>
                        <Style TargetType="TextBlock" x:Key="DescriptionText">
                            <Setter Property="Foreground" Value="#4B5563" />
                            <Setter Property="TextWrapping" Value="Wrap" />
                        </Style>
                    </ItemsControl.Resources>

                    <ItemsControl.Template>
                        <ControlTemplate TargetType="ItemsControl">
                            <ItemsPresenter />
                        </ControlTemplate>
                    </ItemsControl.Template>

                    <Border Style="{StaticResource QueryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Продажі за брендом"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="0,0,0,8" />
                            <TextBlock Style="{StaticResource DescriptionText}"
                                       Text="Аналітика сум продажів по кожній марці та деталізація оплачуваних замовлень конкретного бренду." />
                            <Button Content="Відкрити звіт"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="Btn1_Click" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource QueryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Каталог наявних авто"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="0,0,0,8" />
                            <TextBlock Style="{StaticResource DescriptionText}"
                                       Text="Показує усі доступні продукти, дозволяє перевірити комплектації, ціни та статуси в наявності." />
                            <Button Content="Переглянути"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="Btn2_Click" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource QueryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Затримки постачальників"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="0,0,0,8" />
                            <TextBlock Style="{StaticResource DescriptionText}"
                                       Text="Виявляє бренди та конкретні товари з простроченою доступністю, щоб швидко реагувати на затримки." />
                            <Button Content="Перейти до списку"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="Btn3_Click" />
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource QueryCardStyle}">
                        <StackPanel>
                            <TextBlock Text="Моделі з мінімальним запасом"
                                       FontSize="20"
                                       FontWeight="Bold"
                                       Margin="0,0,0,8" />
                            <TextBlock Style="{StaticResource DescriptionText}"
                                       Text="Показує автомобілі, що наближаються до мінімального рівня складу, з деталями по партіям і доступності." />
                            <Button Content="Перевірити склад"
                                    Style="{StaticResource PrimaryButtonStyle}"
                                    Click="BtnMinStock_Click" />
                        </StackPanel>
                    </Border>
                </ItemsControl>
            </ScrollViewer>
        </StackPanel>
    </Grid>
</Page>
