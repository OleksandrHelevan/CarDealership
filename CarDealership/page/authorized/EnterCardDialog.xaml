<Window x:Class="CarDealership.page.authorized.EnterCardDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Оплата карткою" Height="180" Width="380" WindowStartupLocation="CenterOwner">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical">
            <TextBlock Text="Номер картки" Margin="0,0,0,4"/>
            <TextBox x:Name="CardBox"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Width="90" Margin="0,0,6,0" Content="OK" IsDefault="True" Click="Ok_Click"/>
            <Button Width="90" Content="Скасувати" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>

