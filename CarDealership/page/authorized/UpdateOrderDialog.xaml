<Window x:Class="CarDealership.window.UpdateOrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редагування замовлення" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize" Background="#f8f9fa">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Редагування замовлення" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,10,0,10">
            <TextBlock Text="Тип оплати:" FontWeight="SemiBold"/>
            <ComboBox x:Name="PaymentTypeComboBox" Margin="0,5,0,0" Height="30">
                <ComboBoxItem Content="Готівка" Tag="0"/>
                <ComboBoxItem Content="Картка" Tag="1"/>
                <ComboBoxItem Content="Криптовалюта" Tag="2"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10,0,10">
            <TextBlock Text="Доставка:" FontWeight="SemiBold"/>
            <CheckBox x:Name="DeliveryCheckBox" Content="Потрібна доставка" Margin="0,5,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Зберегти" Width="100" Margin="0,0,10,0"
                    Background="#22C55E" Foreground="White" BorderThickness="0"
                    Click="SaveButton_Click"/>
            <Button Content="Скасувати" Width="100" Background="#EF4444"
                    Foreground="White" BorderThickness="0"
                    Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>