<Page x:Class="CarDealership.page.authorized.GasolineCarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:CarDealership.page.guest"
      xmlns:model="clr-namespace:CarDealership.dto"
      mc:Ignorable="d"
      Title="GasolineCarPage">

    <DockPanel Background="#f7f7f7">

        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="10">
            <TextBox x:Name="SearchBox" Width="200" Height="30" 
                     Margin="0,0,10,0" />
            
            <ComboBox x:Name="FilterComboBox" Width="150" Height="30" Margin="0,0,10,0">
                <ComboBoxItem Content="КПП" IsSelected="True"/>
                <ComboBoxItem Content="Механіка"/>
                <ComboBoxItem Content="Автомат"/>
                <ComboBoxItem Content="CVT"/>
                <ComboBoxItem Content="Напівавтомат"/>
            </ComboBox>

            <Button Content="Застосувати" Width="100" Height="30" Click="ApplyFilter_Click"/>
        </StackPanel>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <ItemsControl x:Name="GasolineCarsList">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type model:GasolineCarDto}">
                        <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="10" Margin="10" Padding="15" Background="#F5F5F5" Width="300">
                            <StackPanel>
                                <TextBlock Text="Бензиновий автомобіль" FontSize="16" FontWeight="Bold" Margin="0 0 0 10"/>
                                
                                <TextBlock Text="{Binding Brand, StringFormat=Марка: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding ModelName, StringFormat=Модель: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding Year, StringFormat=Рік: {0}}" Margin="0 2"/>

                                <TextBlock Text="{Binding Engine.FuelConsumption, StringFormat=Обʼєм двигуна: {0} л}" Margin="0 2"/>
                                <TextBlock Text="{Binding Engine.FuelTypeString, StringFormat=Тип пального: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding Engine.HorsePower, StringFormat=Потужність: {0} к.с.}" Margin="0 2"/>
                                <TextBlock Text="{Binding Engine.Power, StringFormat=Потужність: {0} Вт}" Margin="0 2"/>

                                <TextBlock Text="{Binding ColorString, StringFormat=Колір: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding Mileage, StringFormat=Пробіг: {0} км}" Margin="0 2"/>
                                <TextBlock Text="{Binding Price, StringFormat=Ціна: ${0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding Weight, StringFormat=Вага: {0} кг}" Margin="0 2"/>
                                <TextBlock Text="{Binding TransmissionString, StringFormat=КПП: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding DriveTypeString, StringFormat=Привід: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding NumberOfDoors, StringFormat=Кількість дверей: {0}}" Margin="0 2"/>
                                <TextBlock Text="{Binding BodyTypeString, StringFormat=Тип кузова: {0}}" Margin="0 2"/>

                                <!-- Кнопка Купити -->
                                <Button Content="Купити" 
                                        Background="#3B82F6" Foreground="White" 
                                        Margin="0,10,0,0" Height="35"
                                        Command="{Binding DataContext.BuyCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding}"/>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </DockPanel>
</Page>
